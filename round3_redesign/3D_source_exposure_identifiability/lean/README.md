# Lean status

The 3D theorems are added to the unified workspace at
`/Users/sunlay/Desktop/ood-representation-regularization/formalization`.
This directory is documentation only and does not create a second Lake
project. `SourceExposure.lean` formalizes response additivity, source
reference span invariance, exact exposure membership, and the kernel/image
factorization criterion using the range-restricted observation map.
