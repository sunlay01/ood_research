# Negative Results and Boundaries

The bridge must identify the earliest unstable link in
`family -> A -> O_S -> Pi -> E`.  A partial result is valid when the frozen
head geometry works but a method is degenerate or the full end-to-end network
is unstable.  The result is not a causal mechanism claim, a finite-sample
generalization guarantee, or a target-risk lower bound.
