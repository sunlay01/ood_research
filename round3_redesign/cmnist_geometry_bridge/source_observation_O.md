# Source Observation O_S

The source observation is the stack of source task states
`psi_e=(svec(M_e),m_e,c_e)` for the frozen representation.  It is computed
from source feature moments only.  Its columns are central differences under
the family perturbations.  Target labels, target risk, target response,
regularizer geometry and semantic metadata are absent from construction.
