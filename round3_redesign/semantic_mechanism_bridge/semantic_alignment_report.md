# Semantic alignment report

Status: `NOT-COMPUTABLE-VECTOR-MISSING`. The frozen C/K factorial outputs contain scalar bank norms for contrasts, while semantic alignment requires full functional response vectors and a Gram-aware projection. This bridge therefore refuses to infer cosine, semantic-span mass, or a mechanism from norms. The legacy semantic response table is complete; a future alignment run must archive C/K contrast vectors from the same checkpoint before any alignment claim.

Current verdict: `NO-POSITIVE-SEMANTIC-MECHANISM-CANDIDATE`.
