# Target-Emergent Shift

With source `gamma_U = 0` and target `gamma_U != 0`, the source optimum has
zero `U` coefficient but `E_T[U r*]` is nonzero. This is the formal
source-usage-is-not-relevance counterexample and must be classified as
`unexposed-relevant` when the exposure proxy is low.
