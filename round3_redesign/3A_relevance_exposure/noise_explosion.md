# Noise Explosion

The `K = 0, 4, 16, 64, 256` sweep compares the old ambient moment lifting rank
with the rank and spectrum of whitened target-residual coupling signatures.
Independent noise coordinates are a negative control. Their source weights and
leading relevance should remain zero up to numerical tolerance, while adding
coordinates must not create proportional relevant complexity.
