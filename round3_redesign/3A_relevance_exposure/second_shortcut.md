# Second Shortcut

The benchmark supports two shortcut coordinates. This track records the
resulting additional target-response signatures as a positive-control setting;
it does not call the signatures semantic mechanisms.
