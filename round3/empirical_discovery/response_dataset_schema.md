# Response dataset schema

每条记录包含 model id、regularizer、seed、predictor state、risk-response feature vector、probe names 和 withheld mechanism labels。`blind_features` 只返回标准化 response matrix；metadata 不参与聚类。
