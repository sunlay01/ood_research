# Empirical discovery design

response dataset 的输入只包含模型对预声明 shift probes 的风险响应。shift 覆盖 pure、mixed、hidden-composition、covered/unseen 和 out-of-family。机制标签、regularizer 标签与 target 标签只保留作 post-hoc validation，不进入 blind features。
