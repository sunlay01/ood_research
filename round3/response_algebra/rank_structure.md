# 3A Rank 结构

本轮分别报告 `rank(Phi)`、`rank(Psi)` 和 `rank(R)`，并验证

\[
\operatorname{rank}(\mathsf R)\leq
\min\{\operatorname{rank}(\Phi),\operatorname{rank}(\Psi)\}.
\]

若两侧 rank 不同，不能仅凭 response rank 判断哪一侧具有语义限制；这里只报告 algebraic coverage bottleneck。rank profile 用于检查结论是否依赖单一 tolerance。
