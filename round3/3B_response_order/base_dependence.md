# Base Dependence

The same pure-family directions are evaluated at three base points: the registered nonzero,
anisotropic base; a zero-mean symmetric-ish base; and a second generic base. Changes in rank or
closure across these bases are local-geometry dependence, not evidence for or against a global
mechanism identity.

