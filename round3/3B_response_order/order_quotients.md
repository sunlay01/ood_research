# Order Quotients

The reported quotient dimensions are

`q_k = rank([R^(1),...,R^(k)]) - rank([R^(1),...,R^(k-1)])`.

They measure genuinely new response directions, not larger response magnitude. The current
implementation leaves semantic interpretation to a later track.

