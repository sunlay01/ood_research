# Global Filtration

For order response matrices `R^(k)`, the cumulative spaces are column spans of
`[R^(1), ..., R^(k)]`. Their ranks form a filtration by construction. `new@k` is the rank of the
quotient `V^(<=k) / V^(<=k-1)`. No order factor is assigned a mechanism meaning here.

