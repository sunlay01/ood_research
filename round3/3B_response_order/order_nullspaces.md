# Order Nullspaces

Each order has a lifted statistic matrix `Psi^(k)` and a model-side response matrix
`Phi (Psi^(k))^T`. Their numerical ranks and tolerance profiles are stored in the machine result.
Nullspaces are algebraic reachability diagnostics only; they are not mechanism blind spots.

