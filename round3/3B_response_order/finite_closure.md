# Finite Closure

Finite responses are evaluated at `t=+1` and `t=-1` for every pure direction. The residual after
projection onto the Taylor span through order `K` is recorded as `rho_K`. This explicitly tests
whether a finite response space is generated by the observed differential orders.

