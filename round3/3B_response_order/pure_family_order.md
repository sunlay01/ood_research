# Pure-Family Order

Each registered structural block is evaluated one direction at a time. For each family the
report records individual order ranks, cumulative ranks, rank increments, finite `+/-` span rank,
polynomial degree range, order overlaps, and closure residuals. The finite span includes both
signs; a one-sided finite path would incorrectly omit the even component.

