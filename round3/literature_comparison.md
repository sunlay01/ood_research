# Literature comparison

本轮比较对象按结构而不是关键词组织：causal transportability 的 module stability、causal core/non-core variable partition、Tri-Space 的 representation decomposition、distribution-shift attribution，以及 IRMv1/CORAL 的 operator。

当前判定：这些近邻分别分解 causal modules、representation coordinates、conditions 或 distribution statistics；本轮对象是 `risk state × mechanism tangent` 的 response pairing。该差异仍需逐篇原始定理核验，不能直接写成 novelty claim。
