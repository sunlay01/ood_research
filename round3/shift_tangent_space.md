# Shift tangent space

对结构参数方向 \(\delta\eta\)，解析计算 \(DM(\eta)[\delta\eta]\)。风险响应满足：

\[
dR_f[\delta\eta]=\langle Q_f,DM(\eta)[\delta\eta]\rangle_F.
\]

这是 `theorem`（在当前有限维 smooth structural family 内）。它是 local first-order 结果；有限 shift 使用 exact moment transport，不把一阶式误写成 global theorem。
