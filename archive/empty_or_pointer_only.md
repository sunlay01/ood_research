# Theory branch archive policy

This branch was created from the project history at commit `ad3d8e1` but its working tree was intentionally reduced to a theory research skeleton. The prior code and experiments remain reachable in repository history and on the `ood-algorithm` branch.

No A/O/Pi, C/K, response-geometry, matched-fork, trajectory, or mechanism-memory files were copied here. This is an architectural boundary, not a deletion of archival provenance.
