# Targeted Follow-Up Tasks

1. **Restricted bridge proof:** In a finite-dimensional linear-Gaussian SCM with observed interventions, derive whether IRMv1 stationarity controls a conditional operator radius. Return assumptions and a counterexample if not.
2. **Derivative audit:** Bound empirical-to-population error for a bounded classifier-gradient class; explicitly include `epsilon_opt` and identify when Fishr covariance matching is only diagnostic.
3. **Conditional theorem:** Prove the marginal-plus-conditional decomposition with bounded loss and overlap, then test tightness on the binary shortcut construction.
4. **Target-family audit:** Compare the expected-domain `Pi` bound and the finite-mixture DRO bound under matched source coverage; state which terms are source-estimable versus assumption-controlled.
5. **Novelty check:** Verify whether a typed source-hull/operator decomposition plus explicit fidelity labels already appears in the cited ledger; do not claim novelty without a theorem-level distinction.

Completion gate: proceed to a larger theorem only if at least one bridge is non-vacuous and survives an independent counterexample check. Otherwise report `MAJOR-REPRESENTATION-GAP` or `IMPOSSIBILITY-DIRECTION`.
