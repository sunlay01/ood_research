# Context Template

Current question:

- `<single active question>`

Frozen inputs needed:

- `<result IDs or theorem IDs>`

Required equations:

- `<only equations needed for this task>`

Required numerical facts:

- `<result IDs and values needed>`

Known exclusions:

- Do not reconstruct project history.
- Do not use target risk for source-only training or selection.
- Do not claim causal identification, finite-sample guarantee, target-risk lower
  bound, semantic recovery, or universal DG theorem unless explicitly proved in
  the active task.

Relevant code entrypoints:

- `<source/test files>`

Allowed reopen targets:

- `<historical artifacts, if any>`

Historical reopen:

- `none` unless this context lists explicit targets and reasons.
