# TASK-AOPI-CMNIST-REINSTANTIATION-AUDIT Context

Question: on corrected CPU-minimal ColoredMNIST, do the frozen A/O/Pi objects
remain numerically stable, predict finite source-induced final-head movement,
and discriminate effective IRMv1 from ERM?

The encoder from each verified checkpoint remains frozen. The trainable audit
state is only the augmented final linear head (64 weights plus bias). This is
not a full-network learner-response result.

Trusted inputs:

- Corrected CPU-minimal config and source-generating semantics.
- Verified ERM/IRMv1 checkpoints and manifest for seeds `10..14`.

Source-only construction:

- Source-refined head, source-risk Hessian, observation state, and IFT Pi.
- Three declared source probability coordinates and five derived report directions.

Evaluation-only construction:

- Held-out evaluation worlds define A and assess finite predictions.
- Target rows cannot select any parameter, damping, direction, or threshold.

Canonical state remains unchanged; completion writes only `active/STATE_DELTA.md`.
