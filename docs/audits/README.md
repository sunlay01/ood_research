# Independent Audit Records

`LATENT-001` gate verdicts are generated by
`scripts/run_supervisor_gate.sh`. The supervisor runs in an ephemeral,
read-only Codex context and cannot modify experiment files. JSON verdicts are
append-only evidence; a failed gate may be retried at most once.
